# Atlas = producción compartida (Traefik + Tunnel). Seed → .env en deploy (no pisa .env existente).
DOMAIN=mavora.atlasops.dev
POSTGRES_PASSWORD=mavora_change_me

# Secrets Atlas (Project → Secrets) + atlas.yml envFrom — NO poner valores reales aquí:
#   MAVORA_CRYPTO_SECRET (≥32 chars)
#   DEEPSEEK_API_KEY / LLM_API_KEY / FAL_KEY (opcional; default LLM/FAL=fake)
# Org: git.token / cloudflare.api.token
